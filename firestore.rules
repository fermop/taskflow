rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Regla global para desarrollo: Permitir leer y escribir SOLO si el usuario ha iniciado sesión
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
  }
}